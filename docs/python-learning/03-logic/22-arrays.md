# Python Arrays

Python does not have built-in support for Arrays, but Python Lists can be used instead. Note: To work with arrays in Python you will have to import a library, like the NumPy library.

```python
cars = ["Ford", "Volvo", "BMW"]
print(cars[0])
```
